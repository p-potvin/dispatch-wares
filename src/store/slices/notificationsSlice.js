import { createSlice } from '@reduxjs/toolkit'; const notificationsSlice = createSlice({ name: 'notifications', initialState: { unreadCount: 0, isOpen: false, notifications: [] }, reducers: { toggleNotifications: s => { s.isOpen = !s.isOpen; }, markAllRead: s => { s.unreadCount = 0; }, dismissNotification: (s, action) => { s.notifications = s.notifications.filter(n => n.id !== action.payload); } } }); export const { toggleNotifications, markAllRead, dismissNotification } = notificationsSlice.actions; export default notificationsSlice.reducer;
