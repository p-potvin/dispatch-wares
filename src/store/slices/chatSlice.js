import { createSlice } from '@reduxjs/toolkit'; const chatSlice = createSlice({ name: 'chat', initialState: { unreadCount: 0, messages: [], isOpen: false, activeChannel: null }, reducers: { addMessage: (state, action) => { state.messages.push(action.payload); }, clearUnread: (state) => { state.unreadCount = 0; }, toggleChat: (state) => { state.isOpen = !state.isOpen; }, setActiveChannel: (state, action) => { state.activeChannel = action.payload; } } }); export const { addMessage, clearUnread, toggleChat, setActiveChannel } = chatSlice.actions; export default chatSlice.reducer;
